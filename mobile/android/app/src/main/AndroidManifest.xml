<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Network plugin: drives the "you are offline" banner. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      Push notifications. Required from Android 13 (API 33) onwards, where
      notification permission became a runtime prompt. src/lib/native/push.ts
      requests it contextually rather than at first launch.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
      Dictating a question instead of typing it. Requested at the moment the
      microphone button is pressed, never at launch — see DictateButton, which
      shows what the audio is used for before the prompt appears.

      Both are needed, and both are requested by Capacitor's own
      BridgeWebChromeClient when the page asks for the microphone — it handles
      the runtime prompt and grants the WebView itself, so no MainActivity
      override is wanted here. What it cannot do is request a permission the
      manifest never declared: without these two lines the request fails before
      any dialog appears, which is what "microphone not available" looked like.
    -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!--
      NOTE: no CAMERA or photo-library permissions. Nothing in the app captures or
      picks images yet, and both stores expect every declared permission to be
      justified — an unused one is a review question with no good answer. Re-add
      these alongside @capacitor/camera if a photo-attachment feature is built.

      Sharing a generated chart image needs no permission: @capacitor/share writes
      to the app's own cache directory via @capacitor/filesystem.
    -->

    <!--
      NOTE: no SMS permissions. Firebase phone auth uses the SMS Retriever API,
      which reads only its own verification message and needs no permission.
      Declaring RECEIVE_SMS/READ_SMS would trigger Play's restricted-permission
      review for no benefit.
    -->
</manifest>
